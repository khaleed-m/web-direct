// Global agent tag cleaner\nsetInterval(() => {\n  // Clean all text content on the page\n  document.querySelectorAll('*').forEach(el => {\n    if (el.textContent && el.textContent.includes('Agent')) {\n      el.textContent = el.textContent\n        .replace(/<Agent>/g, '')\n        .replace(/<\\/Agent>/g, '')\n        .replace(/&lt;Agent&gt;/g, '')\n        .replace(/&lt;\\/Agent&gt;/g, '')\n        .trim();\n    }\n  });\n  \n  // Clean shadow DOM elements\n  document.querySelectorAll('elevenlabs-convai').forEach(widget => {\n    if (widget.shadowRoot) {\n      widget.shadowRoot.querySelectorAll('*').forEach(el => {\n        if (el.textContent && el.textContent.includes('Agent')) {\n          el.textContent = el.textContent\n            .replace(/<Agent>/g, '')\n            .replace(/<\\/Agent>/g, '')\n            .replace(/&lt;Agent&gt;/g, '')\n            .replace(/&lt;\\/Agent&gt;/g, '')\n            .trim();\n        }\n      });\n    }\n  });\n}, 500);